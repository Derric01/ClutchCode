{
  "name": "@clutchcode/providers",
  "version": "0.1.0",
  "description": "Provider abstraction: OpenAI-compatible, Anthropic native, Ollama, and FakeProvider (PROJECT_SPEC.md §4.7).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
