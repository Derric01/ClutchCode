{
  "name": "@clutchcode/capability",
  "version": "0.1.0",
  "description": "The capability matrix, the per-model probe, and the edit-format selection algorithm (PROJECT_SPEC.md §4 — \"the heart\").",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@clutchcode/providers": "workspace:*",
    "@clutchcode/tools": "workspace:*",
    "smol-toml": "^1.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
